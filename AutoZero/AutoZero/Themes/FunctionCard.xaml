<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate x:Key="Function">
        <Border Height="25"
                CornerRadius="4"
                Margin="8,2,8,2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <CheckBox Content="Tự động trả lời câu hỏi"
                              Foreground="White"
                              Margin="10,10,0,0"></CheckBox>
                </StackPanel>
            </Grid>
        </Border>
    </DataTemplate>
</ResourceDictionary>